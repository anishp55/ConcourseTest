platform: linux

image_resource:
  type: docker-image
  source:
    repository: microsoft/dotnet
    tag: 1.0.0-preview2-sdk
    
inputs:
  - name: build

run:
  dir: build
  path: sh
  args: 
  - -exc
  - |
    export NUGET_PACKAGES=./packages
    dotnet run